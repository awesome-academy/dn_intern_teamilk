<%= search_form_for @search, url: request.original_url, local: true, method: :get do |f| %>
  <%= f.search_field :name_cont, placeholder: search_text %>
  <%= f.submit t("shared.search"), class: "site-btn-ver-admin" %>
<% end %>
